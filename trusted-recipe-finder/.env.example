# Copy this file to .env.local and add your Anthropic API key
# The app also supports entering the key directly in the Settings tab

# Optional: set API key via environment variable
# VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here
